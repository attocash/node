name: Pipeline

on:
  push:
    branches:
      - *
    branches-ignore:
      - 'release/[0-9]+.x'

jobs:
  build:
    uses: ./.github/workflows/build.yaml
  codeql:
    uses: ./.github/workflows/codeql.yaml
  publish:
    needs:
      - build
      - codeql
    uses: ./.github/workflows/docker.yaml
    secrets:
      token: ${{ secrets.TOKEN }}