# Managed by management-terraform

name: Pull Request

on:
  pull_request:

jobs:
  pipeline:
    permissions:
      actions: read
      contents: read
      security-events: write
    uses: ./.github/workflows/pipeline.yaml
